# Firebase App Hosting backend configuration for microflexfilm.com
runConfig:
  minInstances: 0
  maxInstances: 3
  concurrency: 80
  cpu: 1
  memoryMiB: 512

env:
  - variable: FIREBASE_PROJECT_ID
    value: mfx-2026
    availability:
      - BUILD
      - RUNTIME

  # Gmail SMTP — Google Workspace account that owns the App Password
  # and appears as the sender on inquiry notifications.
  - variable: SMTP_USER
    value: flex@microflexfilm.com
    availability:
      - RUNTIME

  # Google Workspace App Password for flex@microflexfilm.com (16 chars, no spaces).
  # Generate at https://myaccount.google.com/apppasswords while signed in as flex@
  # Set via:  firebase apphosting:secrets:set SMTP_PASSWORD
  - variable: SMTP_PASSWORD
    secret: SMTP_PASSWORD
    availability:
      - RUNTIME

  # Where inquiry emails are sent (comma-separated).
  - variable: INQUIRY_NOTIFY_EMAIL
    value: randy@microflexfilm.com,info@microflexfilm.com
    availability:
      - RUNTIME

  # Display name on the From header.
  - variable: INQUIRY_FROM_NAME
    value: "Microflex Inquiries"
    availability:
      - RUNTIME

  # Tidio live chat public key — drives the chat widget.
  # Find it under Tidio dashboard → Settings → Channels → Live Chat → Installation.
  # Looks like: abc123def456 (~10 alphanumeric chars).
  # Available at BUILD time because Next.js bakes it into the client bundle.
  # Replace REPLACE_WITH_TIDIO_KEY after signing up at https://www.tidio.com
  - variable: NEXT_PUBLIC_TIDIO_PUBLIC_KEY
    value: "REPLACE_WITH_TIDIO_KEY"
    availability:
      - BUILD
      - RUNTIME

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Public marketing site writes inquiries through a server action that uses
    // Firebase Admin SDK (which bypasses these rules). No client-side write
    // access is granted to the `inquiries` collection — keep this locked down.
    match /inquiries/{doc} {
      allow read: if false;
      allow write: if false;
    }

    // Spam/rejected submissions log. Same lockdown — only the server action
    // can write here via Admin SDK.
    match /rejected_inquiries/{doc} {
      allow read: if false;
      allow write: if false;
    }

    // Default deny everything else.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
